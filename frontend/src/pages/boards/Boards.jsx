import styled from "styled-components";
const BoardsSection = styled.div``;
const BoardsWrapper = styled.div``;
const Boards = () => {
  return (
    <BoardsSection>
      <BoardsWrapper>hello</BoardsWrapper>
    </BoardsSection>
  );
};

export default Boards;
